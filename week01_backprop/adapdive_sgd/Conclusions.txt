adaptive_sgd.ipynb
    1) Если вектор-элемент имеет всего две координаты, его можно растянуть до квадратичной формы (когда элементами являются
    исходные координаты и их попарные произведения)

    2) Для бинарной классификации достаточно найти xW и применить к результату поэлементно сигмоидную функцию. Получится
    одномерный вектор с вероятностями принадлежности первому или второму классу каждого элементв (значения лежат между 0 и 1)

    3) Для определения потерь используется функция потерь для логистической регрессии:
    𝐿=−1𝑁∑𝑖𝑦𝑖⋅𝑙𝑜𝑔𝑃(𝑦𝑖|𝑥𝑖,𝑤)+(1−𝑦𝑖)⋅𝑙𝑜𝑔(1−𝑃(𝑦𝑖|𝑥𝑖,𝑤))

    4) Градиент функции потерь считается через chain rule, хорошее объяснение:
    https://medium.com/analytics-vidhya/derivative-of-log-loss-function-for-logistic-regression-9b832f025c2d

    5) Оптимизация весов может проходить разными методами (SGD, SGD with momentum, Nesterov momentum, Adagrad, RMSProp, Adam).
    Особенности описаны в конспекте LectureNotes.txt.
    Особенность реализации:
    По конспекту, начиная с Adagrad, после подсчета новых средних скользящих при вычислении новой матрицы весов используются
    старые средние скользящие с прошлой итерации. При реализации это неудобно, поэтому я использовал только что вычисленные
    средние скользящие. Неизвестно, насколько сильно это портит результаты.